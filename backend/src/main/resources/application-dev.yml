spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ticketmgmt}
    username: ${DB_USERNAME:ticketmgmt}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
